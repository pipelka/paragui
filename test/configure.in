dnl Process this file with autoconf to produce a configure script.
AC_INIT(README)

dnl Setup for automake
SDL_VERSION=1.2.0
PARAGUI_VERSION=1.0.0

dnl Detect the canonical host and target build environment
AC_CANONICAL_HOST
AC_CANONICAL_TARGET

AM_INIT_AUTOMAKE(paratest, $PARAGUI_VERSION)

dnl Check for tools

AC_PROG_MAKE_SET
AC_PROG_CC
AC_PROG_CXX
AC_PROG_CPP
AC_PROG_INSTALL

dnl Check for compiler environment

AC_C_CONST

case "$target" in
    *-*-cygwin | *-*-mingw32*)
	ac_default_prefix=/usr/local/cross-tools/i386-mingw32msvc
	AC_MINGW32
	AC_EXEEXT
        ;;
    *-*-beos*)
	ac_default_prefix=/boot/develop/tools/gnupro
	;;
esac

dnl Check for PARAGUI

AM_PATH_PARAGUI($PARAGUI_VERSION,
            :,
	    AC_MSG_ERROR([*** PARAGUI version $PARAGUI_VERSION not found!])
)
#AC_ARG_ENABLE(static,[  --enable-static         Try to build statically.], [
#  if test "$enable_static" = "yes"; then 
#	static=yes
#  fi
#])
#if test "$static" != "no"; then
#   PARAGUI_LIBS="-static `$PARAGUI_CONFIG --static-libs`"
#fi
CXXFLAGS="$CFLAGS"
AC_SUBST(CXXFLAGS)

# Finally create all the generated files
AC_OUTPUT([
Makefile
])
